import Layout from "../components/Layout";

const notFound = () => {
    return (
        <Layout>
            <div>
                <p>This is an error page</p>
            </div>
        </Layout>
    );
};

export default notFound;
